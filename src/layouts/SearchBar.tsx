import React from "react";

const SearchBar = () => {
  return (
    <div className="fixed top-0 left-[300px] h-[64px] w-full border-2 border-dotted bg-light">
      This is search bar
    </div>
  );
};

export default SearchBar;
